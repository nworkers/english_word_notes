# english_memory_note_maker

영어 단어가 포함된 이미지 파일을 웹페이지에서 업로드하고, 이를 바탕으로 암기 노트 형태의 단어장을 생성하는 웹 서비스 프로젝트입니다.

## 프로젝트 문서

- [사양 명세](docs/PROJECT_SPEC.md)
- [설계 문서](docs/DESIGN.md)
- [작업 기록](docs/work-logs/README.md)

## 프로젝트 개요

- 웹페이지에서 여러 개의 `jpg` 또는 `png` 이미지 파일을 업로드할 수 있습니다.
- 업로드된 이미지 안의 영어 단어와 뜻을 읽어 단어-뜻 쌍으로 정리합니다.
- 정리된 내용을 바탕으로 웹에서 단어장을 확인하고 프린터로 출력할 수 있습니다.
- 암기 노트는 다음 두 가지 형식을 모두 포함합니다.
  - 단어만 보고 뜻을 쓰는 형식
  - 뜻만 보고 단어를 쓰는 형식
- 출력 결과는 학습자가 직접 손으로 적을 수 있도록 인쇄에 적합한 여백과 줄 간격을 고려해야 합니다.

## 현재 문서 상태

- 핵심 요구사항 반영 완료
- 설계 방향 초안 반영 완료
- `Next.js + TypeScript` 초기 코드 초안 추가 완료

## 현재 구현 초안

- `app/` 기반 Next.js App Router 구조
- 다중 이미지 업로드 UI
- 업로드 요청을 받는 `/api/extract` API
- `tesseract.js` 기반 OCR 추출 로직
- 브라우저 인쇄를 고려한 단어장 미리보기 스타일
- OCR 원문 미리보기 및 추출 경고 메시지 표시

## 다음 단계

1. 웹 서비스의 화면 구성과 업로드 흐름을 정합니다.
2. OCR 방식과 정확도 보정 규칙을 정합니다.
3. 프린터 출력에 적합한 단어장 레이아웃을 구체화합니다.

## 실행 예정 방법

```bash
npm install
npm run dev
```

현재 작업 환경에서는 `npm install`과 빌드 검증을 완료했습니다.

## OCR 메모

- 현재 OCR은 서버에서 `tesseract.js`로 처리합니다.
- 이미지에서 영어 단어와 한글 뜻을 읽어 단어-뜻 쌍을 추출하도록 시도합니다.
- OCR 결과가 애매한 경우 원문 미리보기와 경고 메시지를 함께 보여줍니다.

## Ollama 연동

- 선택적으로 로컬 `Ollama`를 후처리 단계에 연결할 수 있습니다.
- 이 경우 흐름은 `OCR -> Ollama 구조화 -> 웹 반환` 입니다.
- 기본값은 비활성화이며, 아래 환경 변수를 설정하면 사용합니다.

```bash
OLLAMA_ENABLED=true
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma4:e2b
OLLAMA_VISION_MODEL=gemma4:e4b
```

- `Ollama` 호출에 실패하면 서비스는 중단되지 않고 OCR-only 결과로 자동 폴백합니다.
- Vision-only 모드는 `Ollama`가 이미지 입력을 지원하는 모델일 때만 동작합니다.
